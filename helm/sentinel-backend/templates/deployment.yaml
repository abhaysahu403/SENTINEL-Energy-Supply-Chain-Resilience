apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sentinel-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentinel-backend.labels" . | nindent 4 }}
    tier: application
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  strategy:
    {{- toYaml .Values.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "sentinel-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "sentinel-backend.selectorLabels" . | nindent 8 }}
        tier: application
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "sentinel-backend.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      containers:
        - name: backend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          env:
            - name: SENTINEL_AUTOPLAY
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: sentinel-autoplay
            - name: SENTINEL_SIM_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: sentinel-sim-interval
            - name: SENTINEL_DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: database-url
            - name: SENTINEL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "sentinel-backend.secretName" . }}
                  key: jwt-secret
            - name: PYTHONUNBUFFERED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: python-unbuffered
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: log-level
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: {{ include "sentinel-backend.fullname" . }}
                  key: app-env
          {{- if .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.startupProbe }}
          startupProbe:
            {{- toYaml .Values.startupProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
