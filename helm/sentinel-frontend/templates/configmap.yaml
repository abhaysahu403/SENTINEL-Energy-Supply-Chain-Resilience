apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sentinel-frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sentinel-frontend.labels" . | nindent 4 }}
data:
  # Backend API URL for frontend
  api-base-url: {{ .Values.config.apiBaseUrl | quote }}
