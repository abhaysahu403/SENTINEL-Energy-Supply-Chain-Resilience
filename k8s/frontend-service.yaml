apiVersion: v1
kind: Service
metadata:
  name: sentinel-frontend
  namespace: sentinel
  labels:
    app: sentinel
    component: frontend
    tier: presentation
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: sentinel
    component: frontend
